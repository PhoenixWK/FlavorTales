app:
  r2:
    account-id: ${R2_ACCOUNT_ID:dev-account-id}
    access-key: ${R2_ACCESS_KEY:dev-access-key}
    secret-key: ${R2_SECRET_KEY:dev-secret-key}
    bucket: flavortales
    public-url: ${R2_PUBLIC_URL:https://cdn.flavortales.site}
